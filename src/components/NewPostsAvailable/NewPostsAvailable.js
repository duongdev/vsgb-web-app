import React from 'react';
import PropTypes from 'prop-types';
import Button from 'material-ui/Button';

const NewPostsAvailable = () => (
  <div>
    <Button color="accent">20 new photos</Button>
  </div>);

NewPostsAvailable.propTypes = {};
NewPostsAvailable.defaultProps = {};

export default NewPostsAvailable;
